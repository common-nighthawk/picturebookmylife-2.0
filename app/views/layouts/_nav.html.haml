%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      %a.navbar-brand{:href => "/"}
        PictureBookMyLife
    %ul.nav.navbar-nav.navbar-right.hidden-xs
      - if current_user
        %li
          = link_to "Log Out", destroy_user_session_path, method: :delete
        %li
          = link_to "Your Books, #{current_user.username}", user_books_path(current_user)
      - else
        %li
          = link_to "sign in", root_path
